--- a/src/mame/sega/model2_v.cpp
+++ b/src/mame/sega/model2_v.cpp
@@ -2302,6 +2302,8 @@
 
 u32 *model2_state::geo_process_command(geo_state *geo, u32 opcode, u32 *input, bool *end_code)
 {
+	if (getenv("VON_GEO_CMD_TRACE"))
+		fprintf(stderr, "VONCMD %.6f %02x\n", machine().time().as_double(), (opcode >> 23) & 0x1f);
 	switch ((opcode >> 23) & 0x1f)
 	{
 		case 0x00: input = geo_nop(geo, opcode, input);                   break;
