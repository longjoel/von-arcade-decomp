diff --git a/src/emu/emuopts.cpp b/src/emu/emuopts.cpp
index 33549b4e..b3c1a55d 100644
--- a/src/emu/emuopts.cpp
+++ b/src/emu/emuopts.cpp
@@ -193,6 +193,7 @@ const options_entry emu_options::s_option_entries[] =
 	{ OPTION_COMM_REMOTE_HOST,                           "127.0.0.1", core_options::option_type::STRING,     "remote address to connect to" },
 	{ OPTION_COMM_REMOTE_PORT,                           "15112",     core_options::option_type::STRING,     "remote port to connect to" },
 	{ OPTION_COMM_FRAME_SYNC,                            "0",         core_options::option_type::BOOLEAN,    "sync frames" },
+	{ OPTION_COMM_MASTER,                                "0",         core_options::option_type::BOOLEAN,    "make the Model 2 communication board the link master" },
 
 	// misc options
 	{ nullptr,                                           nullptr,     core_options::option_type::HEADER,     "CORE MISC OPTIONS" },
diff --git a/src/emu/emuopts.h b/src/emu/emuopts.h
index af8ff513..fde97bb2 100644
--- a/src/emu/emuopts.h
+++ b/src/emu/emuopts.h
@@ -183,6 +183,7 @@
 #define OPTION_COMM_REMOTE_HOST     "comm_remotehost"
 #define OPTION_COMM_REMOTE_PORT     "comm_remoteport"
 #define OPTION_COMM_FRAME_SYNC      "comm_framesync"
+#define OPTION_COMM_MASTER           "comm_master"
 
 #define OPTION_CONFIRM_QUIT         "confirm_quit"
 #define OPTION_UI_MOUSE             "ui_mouse"
@@ -466,6 +467,7 @@ public:
 	const char *comm_remotehost() const { return value(OPTION_COMM_REMOTE_HOST); }
 	const char *comm_remoteport() const { return value(OPTION_COMM_REMOTE_PORT); }
 	bool comm_framesync() const { return bool_value(OPTION_COMM_FRAME_SYNC); }
+	bool comm_master() const { return bool_value(OPTION_COMM_MASTER); }
 
 	bool confirm_quit() const { return bool_value(OPTION_CONFIRM_QUIT); }
 	bool ui_mouse() const { return bool_value(OPTION_UI_MOUSE); }
diff --git a/src/frontend/mame/ui/ui.cpp b/src/frontend/mame/ui/ui.cpp
index cb3abef6..d15b6a40 100644
--- a/src/frontend/mame/ui/ui.cpp
+++ b/src/frontend/mame/ui/ui.cpp
@@ -658,7 +658,7 @@ void mame_ui_manager::display_startup_screens(bool first_time)
 	const int maxstate = 3;
 	int const str = machine().options().seconds_to_run();
 	bool show_gameinfo = !machine().options().skip_gameinfo();
-	bool show_warnings = true;
+	bool show_warnings = !machine().options().skip_gameinfo();
 	bool video_none = strcmp(downcast<osd_options &>(machine().options()).video(), OSDOPTVAL_NONE) == 0;
 
 	// disable everything if we are using -str for 300 or fewer seconds, or if we're the empty driver,
diff --git a/src/mame/mame.lst b/src/mame/mame.lst
index bf122c0b..1304e87a 100644
--- a/src/mame/mame.lst
+++ b/src/mame/mame.lst
@@ -40943,6 +40943,7 @@ vf2b
 vf2o
 von
 vonj
+vonjdev
 vonr
 vonu
 vstriker
diff --git a/src/mame/sega/m2comm.cpp b/src/mame/sega/m2comm.cpp
index f590e0d3..43943030 100644
--- a/src/mame/sega/m2comm.cpp
+++ b/src/mame/sega/m2comm.cpp
@@ -212,6 +212,7 @@ m2comm_device::m2comm_device(const machine_config &mconfig, const char *tag, dev
 	strcat(m_remotehost, mconfig.options().comm_remoteport());
 
 	m_framesync = mconfig.options().comm_framesync() ? 0x01 : 0x00;
+	m_master = mconfig.options().comm_master();
 
 	m_frameoffset = 0x1c0; // default
 }
@@ -299,7 +300,7 @@ void m2comm_device::cn_w(uint8_t data)
 			m_shared[i] = 0x00;
 		}
 
-		m_shared[0x01] = 0x02;
+		m_shared[0x01] = m_master ? 0x01 : 0x02;
 		// TODO - check EPR-16726 on Daytona USA and Sega Rally Championship
 		// EPR-18643(A) - these are accessed by VirtuaON and Sega Touring Car Championship
 
diff --git a/src/mame/sega/m2comm.h b/src/mame/sega/m2comm.h
index e6a3dfe1..af9230a4 100644
--- a/src/mame/sega/m2comm.h
+++ b/src/mame/sega/m2comm.h
@@ -63,6 +63,7 @@ private:
 	uint8_t m_buffer0[0x1000]{};
 	uint8_t m_buffer1[0x1000]{};
 	uint8_t m_framesync;
+	bool m_master = false;
 	uint16_t m_frameoffset;
 
 #ifdef M2COMM_SIMULATION
diff --git a/src/mame/sega/model2.cpp b/src/mame/sega/model2.cpp
index f7dc5064..dbc3990e 100644
--- a/src/mame/sega/model2.cpp
+++ b/src/mame/sega/model2.cpp
@@ -646,5 +646,12 @@ void model2b_state::copro_sharc_buffer_w(offs_t offset, u32 data)
 	m_bufferram[offset & 0x7fff] = data;
 }
 
+void model2b_state::geo_sharc_iop_w(offs_t offset, u32 data)
+{
+	// The Virtual-On host initializes this Geo/SHARC IOP window, but the
+	// register meanings and side effects are not established yet.
+	logerror("geo_sharc_iop_w: PC:%08x address:%04x data:%08x\n", m_maincpu->pc(), offset * 4, data);
+}
+
 void model2b_state::copro_sharc_map(address_map &map)
 {
@@ -673,5 +673,12 @@ u32 model2b_state::copro_fifo_r()
 u32 model2b_state::copro_fifo_r()
 {
-	return m_copro_fifo_out->pop();
+	u32 data = m_copro_fifo_out->pop();
+	static unsigned vonj_copro_response_trace_count;
+	if ((machine().system().name == std::string_view("vonj")) && (vonj_copro_response_trace_count < 256))
+	{
+		logerror("vonj_copro_response: pc=%08x data=%08x\n", m_maincpu->pc(), data);
+		vonj_copro_response_trace_count++;
+	}
+	return data;
 }

@@ -677,21 +684,33 @@ void model2b_state::copro_fifo_w(u32 data)
 void model2b_state::copro_fifo_w(u32 data)
 {
 	if (m_coproctl & 0x80000000)
 	{
 		m_copro_adsp->external_dma_write(m_coprocnt, data & 0xffff);
 		m_coprocnt++;
 	}
 	else
 	{
+		static unsigned vonj_copro_fifo_trace_count;
+		if ((machine().system().name == std::string_view("vonj")) && ((vonj_copro_fifo_trace_count < 256) || (data != 8)))
+		{
+			logerror("vonj_copro_fifo: pc=%08x data=%08x\n", m_maincpu->pc(), data);
+			vonj_copro_fifo_trace_count++;
+		}
 		m_copro_fifo_in->push(u32(data));
 	}
 }
 
 void model2b_state::copro_function_port_w(offs_t offset, u32 data)
 {
+	static unsigned vonj_copro_function_trace_count;
 	u32 d = data & 0x800fffff;
 	u32 a = (offset >> 2) & 0xff;
 	d |= a << 23;
+	if ((machine().system().name == std::string_view("vonj")) && (vonj_copro_function_trace_count < 256))
+	{
+		logerror("vonj_copro_function: pc=%08x offset=%04x data=%08x normalized=%08x\n", m_maincpu->pc(), offset * 4, data, d);
+		vonj_copro_function_trace_count++;
+	}
 
 	m_copro_fifo_in->push(u32(d));
 }
@@ -791,7 +809,7 @@ void model2_state::geo_prg_w(u32 data)
 {
 	if (m_geoctl & 0x80000000)
 	{
-		//logerror("geo_prg_w: %08X:   %08X\n", m_geocnt, data);
+		logerror("geo_prg_data: %04x\n", data & 0xffff);
 		m_geocnt++;
 	}
 	else
@@ -822,8 +829,14 @@ u32 model2_state::geo_r(offs_t offset)
 void model2_state::geo_w(offs_t offset, u32 data)
 {
 	int address = offset * 4;
+	static unsigned vonj_geo_trace_count;
 
 	if (address < 0x1000)
 	{
+		if ((machine().system().name == std::string_view("vonj")) && (m_geocnt > 0) && ((data != 0) || (vonj_geo_trace_count < 32)) && (vonj_geo_trace_count < 512))
+		{
+			logerror("vonj_geo_cmd: pc=%08x address=%04x data=%08x ctl=%08x\n", m_maincpu->pc(), address, data, m_geoctl);
+			vonj_geo_trace_count++;
+		}
 
 		/*if (data & 0x80000000)
@@ -1385,7 +1392,7 @@ void model2b_state::model2b_crx_mem(address_map &map)
 
 	map(0x00804000, 0x00807fff).rw(FUNC(model2b_state::geo_prg_r), FUNC(model2b_state::geo_prg_w));
 	//map(0x00804000, 0x00807fff).rw(FUNC(model2b_state::geo_sharc_fifo_r), FUNC(model2b_state::geo_sharc_fifo_w));
-	//map(0x00840000, 0x00840fff).w(FUNC(model2b_state::geo_sharc_iop_w));
+	map(0x00840000, 0x00840fff).w(FUNC(model2b_state::geo_sharc_iop_w));
 
 	map(0x00880000, 0x00883fff).w(FUNC(model2b_state::copro_function_port_w));
 	map(0x00884000, 0x00887fff).rw(FUNC(model2b_state::copro_fifo_r), FUNC(model2b_state::copro_fifo_w));
@@ -1404,7 +1411,7 @@ void model2b_state::model2b_crx_mem(address_map &map)
 	map(0x11300000, 0x113fffff).ram().share("textureram1").flags(i960_cpu_device::BURST); // texture RAM 1 (2b/2c)
 	map(0x11400000, 0x1140ffff).rw(FUNC(model2b_state::lumaram_r), FUNC(model2b_state::lumaram_w)).umask16(0x00ff).flags(i960_cpu_device::BURST);    // polygon "luma" RAM (2b/2c)
 
-	map(0x01c00000, 0x01c0001f).rw("io", FUNC(sega_315_5649_device::read), FUNC(sega_315_5649_device::write)).umask32(0x00ff00ff);
+	map(0x01c00000, 0x01c0001f).mirror(0x00000200).rw("io", FUNC(sega_315_5649_device::read), FUNC(sega_315_5649_device::write)).umask32(0x00ff00ff);
 	map(0x01c00040, 0x01c00043).nopw();
 }
 
@@ -2436,6 +2443,9 @@ void model2_state::screen_vblank(int state)
 	}
 	if (m_m2comm)
 		m_m2comm->check_vint_irq();
+
+	if ((machine().system().name == std::string_view("vonjdev")) && (m_workram[0] == 0x49393630U))
+		popmessage("i960 C prototype is running; video/game initialization is not implemented");
 }
 
 void model2_state::sound_ready_w(int state)
@@ -2722,7 +2732,7 @@ void model2a_state::model2a(machine_config &config)
 	io.in_pc_callback().set_ioport("IN1");
 	io.in_pd_callback().set_ioport("IN2");
 	io.in_pg_callback().set_ioport("SW");
-	io.out_pe_callback().set([this] (u8 data) { m_billboard->write(data); });
+	io.out_pe_callback().set([this] (u8 data) { if (m_billboard) m_billboard->write(data); });
 	io.out_pf_callback().set(FUNC(model2_state::lamp_output_w));
 
 	model2_timers(config);
@@ -2860,7 +2870,7 @@ void model2b_state::model2b(machine_config &config)
 	io.in_pc_callback().set_ioport("IN1");
 	io.in_pd_callback().set_ioport("IN2");
 	io.in_pg_callback().set_ioport("SW");
-	io.out_pe_callback().set([this] (u8 data) { m_billboard->write(data); });
+	io.out_pe_callback().set([this] (u8 data) { if (m_billboard) m_billboard->write(data); });
 	io.out_pf_callback().set(FUNC(model2_state::lamp_output_w));
 
 	model2_timers(config);
@@ -2874,6 +2884,14 @@ void model2b_state::model2b(machine_config &config)
 	config.set_default_layout(layout_segabill);
 }
 
+void model2b_state::von(machine_config &config)
+{
+	model2b(config);
+
+	// Virtual-On does not use the optional Versus City billboard controller.
+	config.device_remove("billboard");
+}
+
 void model2b_state::model2b_5881(machine_config &config)
 {
 	model2b(config);
@@ -6371,6 +6389,43 @@ ROM_START( vonj ) /* Virtual On Cyber Troopers Revision B (Japan), Model 2B, Seg
 	ROM_LOAD16_WORD_SWAP("mpr-18653.34", 0x400000, 0x400000, CRC(9ec3e7bf) SHA1(197bc8adc823e93128c1cebf69361a7c7297f808) )
 ROM_END
 
+ROM_START( vonjdev ) /* Development host ROM; all non-host regions remain from vonj. */
+	ROM_REGION( 0x200000, "maincpu", ROMREGION_ERASEFF ) // custom i960 prototype
+	ROM_LOAD("prototype-maincpu.bin", 0x000000, 0x200000, NO_DUMP)
+
+	ROM_REGION32_LE( 0x2000000, "main_data", 0 ) // Data
+	ROM_LOAD32_WORD("mpr-18648.11", 0x000000, 0x400000, CRC(2edbe489) SHA1(ded2e4b295be08970d13c387818c570c3afe8109) )
+	ROM_LOAD32_WORD("mpr-18649.12", 0x000002, 0x400000, CRC(e68c5aa6) SHA1(cdee1ba9247eda4282442d0522f8de7d7c86e1e6) )
+	ROM_LOAD32_WORD("mpr-18650.9",  0x800000, 0x400000, CRC(89a855b9) SHA1(5096db1da1f7e175000e89fca2a1dd3fd53030ea) )
+	ROM_LOAD32_WORD("mpr-18651.10", 0x800002, 0x400000, CRC(f4c23107) SHA1(f65984614111b12dd414db80751efe64fcf5ef16) )
+
+	ROM_REGION32_LE( 0x800000, "copro_data", 0 ) // Copro extra data
+	ROM_LOAD32_WORD("mpr-18662.29", 0x000000, 0x200000, CRC(a33d3335) SHA1(991bbe9dcbef8bfa96682e9d142623fc9b7c0879) )
+	ROM_LOAD32_WORD("mpr-18663.30", 0x000002, 0x200000, CRC(ea74a641) SHA1(a684e13c0afe2ef3f3108ae9b73389121368fc4e) )
+
+	ROM_REGION( 0x1000000, "polygons", ROMREGION_ERASEFF ) // Models
+	ROM_LOAD32_WORD("mpr-18654.17", 0x000000, 0x400000, CRC(6a0caf29) SHA1(9f009f44e62ae0f9dec7a34a163bc186d1c4cbbd) )
+	ROM_LOAD32_WORD("mpr-18655.21", 0x000002, 0x400000, CRC(a4293e78) SHA1(af512c994bedbdaf3a5eeed607e771dcd87810fc) )
+	ROM_LOAD32_WORD("mpr-18656.18", 0x800000, 0x400000, CRC(b4f51e76) SHA1(eb71ada331576f2a7219d238ea07a61bcbf6381a) )
+	ROM_LOAD32_WORD("mpr-18657.22", 0x800002, 0x400000, CRC(a9be4674) SHA1(a918c2a3de78a08104480097edfb9d6aeaeda873) )
+
+	ROM_REGION( 0x1000000, "textures", ROMREGION_ERASEFF ) // Textures
+	ROM_LOAD32_WORD("mpr-18660.27", 0x000000, 0x200000, CRC(e53663e5) SHA1(0a4908be654bad4f00d7d58f0e42f631996911c9) )
+	ROM_LOAD32_WORD("mpr-18658.25", 0x000002, 0x200000, CRC(3d0fcd01) SHA1(c8626c879bfcf7abd095cac5dc03a04ae8629423) )
+	ROM_LOAD32_WORD("mpr-18661.28", 0x800000, 0x200000, CRC(52b50410) SHA1(64ea7b2f86745954e0b8a15d71203444705240a2) )
+	ROM_LOAD32_WORD("mpr-18659.26", 0x800002, 0x200000, CRC(27aa8ae2) SHA1(e9b756e5b4b1c19e52e47af03c773fee544be420) )
+
+	ROM_REGION( 0x20000, "cpu3", 0) // Communication program
+	ROM_LOAD16_WORD_SWAP("epr-18643a.7", 0x000000, 0x20000, CRC(b5e048ec) SHA1(8182e05a2ffebd590a936c1359c81e60caa79c2a) )
+
+	ROM_REGION( 0x080000, "audiocpu", 0 ) // Sound program
+	ROM_LOAD16_WORD_SWAP("epr-18670.31", 0x000000, 0x080000, CRC(3e715f76) SHA1(4fd997e379a8cdb94ec3b1986b3ab443fc6fa12a) )
+
+	ROM_REGION16_BE( 0x800000, "samples", 0 ) // Samples
+	ROM_LOAD16_WORD_SWAP("mpr-18652.32", 0x000000, 0x400000, CRC(037eee53) SHA1(e592f9e97abe0a7bc9009d8327b93da9bc43749c) )
+	ROM_LOAD16_WORD_SWAP("mpr-18653.34", 0x400000, 0x400000, CRC(9ec3e7bf) SHA1(197bc8adc823e93128c1cebf69361a7c7297f808) )
+ROM_END
+
 ROM_START( vonr ) /* Virtual On Cyber Troopers Relay (Japan) */
 	ROM_REGION( 0x200000, "maincpu", 0 ) // i960 program
 	ROM_LOAD32_WORD("epr-18671.15",  0x000000, 0x080000, CRC(fdc01c6b) SHA1(27f10bf02f31688009041b7c85a9527408adcb59) )
@@ -7672,10 +7727,11 @@ GAME( 1995, gunblade,   0,        gunblade,     gunblade,  model2b_state, empty_
 GAMEL(1995, indy500,    0,        indy500,      indy500,   model2b_state, empty_init,    ROT0, "Sega",   "INDY 500 Twin (Revision A, Newer)", MACHINE_NOT_WORKING, layout_vr)
 GAMEL(1995, indy500d,   indy500,  indy500,      indy500,   model2b_state, empty_init,    ROT0, "Sega",   "INDY 500 Deluxe (Revision A)", MACHINE_NOT_WORKING, layout_vr)
 GAMEL(1995, indy500to,  indy500,  indy500,      indy500,   model2b_state, empty_init,    ROT0, "Sega",   "INDY 500 Twin (Revision A)", MACHINE_NOT_WORKING, layout_vr)
-GAME( 1995, von,        0,        model2b,      von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Twin (Export)", MACHINE_NOT_WORKING )
-GAME( 1995, vonu,       von,      model2b,      von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Twin (USA, Revision B)", MACHINE_NOT_WORKING )
-GAME( 1995, vonj,       von,      model2b,      von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Twin (Japan, Revision B)", MACHINE_NOT_WORKING )
-GAME( 1995, vonr,       von,      model2b,      von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Relay (Japan)", MACHINE_NOT_WORKING )
+GAME( 1995, von,        0,        von,          von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Twin (Export)", MACHINE_NOT_WORKING )
+GAME( 1995, vonu,       von,      von,          von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Twin (USA, Revision B)", MACHINE_NOT_WORKING )
+GAME( 1995, vonj,       von,      von,          von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Twin (Japan, Revision B)", MACHINE_NOT_WORKING )
+GAME( 1995, vonjdev,    0,        von,          von,       model2b_state, empty_init,    ROT0, "Research", "Cyber Troopers Virtual-On - i960 C prototype", MACHINE_NOT_WORKING )
+GAME( 1995, vonr,       von,      von,          von,       model2b_state, empty_init,    ROT0, "Sega",   "Cyber Troopers Virtual-On - Relay (Japan)", MACHINE_NOT_WORKING )
 GAME( 1996, schamp,     0,        model2b,      schamp,    model2b_state, empty_init,    ROT0, "Sega",   "Sonic Championship (USA)", MACHINE_NOT_WORKING )
 GAME( 1996, sfight,     schamp,   model2b,      schamp,    model2b_state, empty_init,    ROT0, "Sega",   "Sonic the Fighters (Japan)", MACHINE_NOT_WORKING )
 GAME( 1996, lastbrnx,   0,        model2b,      vf2,       model2b_state, empty_init,    ROT0, "Sega",   "Last Bronx (Export, Revision A)", MACHINE_NOT_WORKING )
diff --git a/src/mame/sega/model2.h b/src/mame/sega/model2.h
index fb59a458..e0ed411a 100644
--- a/src/mame/sega/model2.h
+++ b/src/mame/sega/model2.h
@@ -538,6 +538,7 @@ public:
 	{}
 
 	void model2b(machine_config &config) ATTR_COLD;
+	void von(machine_config &config) ATTR_COLD;
 	void model2b_0229(machine_config &config) ATTR_COLD;
 	void model2b_5881(machine_config &config) ATTR_COLD;
 	void indy500(machine_config &config) ATTR_COLD;
@@ -558,6 +559,7 @@ protected:
 	void copro_fifo_w(u32 data);
 	u32 copro_sharc_buffer_r(offs_t offset);
 	void copro_sharc_buffer_w(offs_t offset, u32 data);
+	void geo_sharc_iop_w(offs_t offset, u32 data);
 
 	void model2b_crx_mem(address_map &map) ATTR_COLD;
 	void model2b_5881_mem(address_map &map) ATTR_COLD;
@@ -572,7 +574,7 @@ protected:
 	virtual void copro_boot() override;
 
 private:
-	required_device<sega_billboard_device> m_billboard;
+	optional_device<sega_billboard_device> m_billboard;
 };
 
 /*****************************
diff --git a/src/mame/sega/segaic24.cpp b/src/mame/sega/segaic24.cpp
index f94f4b7a..80835bee 100644
--- a/src/mame/sega/segaic24.cpp
+++ b/src/mame/sega/segaic24.cpp
@@ -530,6 +530,12 @@ uint16_t segas24_tile_device::char_r(offs_t offset)
 
 void segas24_tile_device::tile_w(offs_t offset, uint16_t data, uint16_t mem_mask)
 {
+	static unsigned vonj_trace_count;
+	if (((machine().system().name == std::string_view("vonj")) || (machine().system().name == std::string_view("vonjdev"))) && data && (offset < 0x1000) && (vonj_trace_count < 4096))
+	{
+		logerror("vonj_tile_write: pc=%08x offset=%04x data=%04x mask=%04x\n", machine().device<cpu_device>("maincpu")->pc(), offset, data, mem_mask);
+		vonj_trace_count++;
+	}
 	COMBINE_DATA(tile_ram.get() + offset);
 	if(offset < 0x4000)
 		tile_layer[offset >> 12]->mark_tile_dirty(offset & 0xfff);
