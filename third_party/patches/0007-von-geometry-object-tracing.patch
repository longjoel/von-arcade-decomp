diff --git a/src/mame/sega/model2_v.cpp b/src/mame/sega/model2_v.cpp
--- a/src/mame/sega/model2_v.cpp
+++ b/src/mame/sega/model2_v.cpp
@@ -1733,3 +1733,11 @@
 	u32  obc = *input++;     /* Object Count */
 
 	u32 *obp;                /* Object Pointer */
+	static unsigned vonj_geometry_object_trace_count;
+	if ((machine().system().name == std::string_view("vonj")) && (vonj_geometry_object_trace_count < 2048))
+	{
+		logerror("vonj_geometry_object: tpa=%08x tha=%08x oba=%08x count=%08x mode=%u source=%s\n",
+			tpa, tha, oba, obc, geo->mode & 3,
+			(oba & 0x01000000) ? "fast-ram" : (oba & 0x00800000) ? "polygon-rom" : "slow-ram");
+		vonj_geometry_object_trace_count++;
+	}
