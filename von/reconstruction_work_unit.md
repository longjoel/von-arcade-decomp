# Reconstruction Work Unit

Use one copy of this template for each bounded routine or tightly coupled
routine group translated from firmware to C.

## Identity

- Slice name:
- Firmware image:
- Subsystem:
- ROM range: `0x` - `0x` (exclusive)
- Byte count:
- Original entry point/callers:

## Classification

- Code/data boundary evidence:
- Calling convention and register roles:
- External memory/MMIO references:
- Unresolved assumptions:

## Implementation

- C source and symbol:
- Target compiler/toolchain:
- Link address and build command:
- Assembly glue required:

## Validation

- Original byte checksum:
- Generated byte checksum:
- Byte comparison command and result:
- Runtime/trace regression command and result:
- Earliest failure stage, if not passing:

## Completion

Mark exactly one:

- `byte-validated`: C build matches the original bytes and regression passes.
- `provisional`: C builds and/or behavior matches, but byte match is incomplete.
- `blocked`: evidence or hardware behavior prevents a safe translation.

Update `von/reconstruction_ledger.json` only after the evidence is captured.
